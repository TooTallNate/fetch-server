{
  "name": "fetch-server",
  "version": "0.0.4",
  "description": "HTTP server framework that mirrors the Fetch API",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "fetch",
    "api",
    "server",
    "json",
    "framework"
  ],
  "author": "Nathan Rajlich <n@n8.io>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "typescript": "^4.0.3"
  }
}
